
//  function Mapfunct() {
//    const originalArray = [1, 2, 3, 4, 5];

//    const modifiedArray1 = originalArray.map((number) => number * 2);

//    originalArray.map((number, index, array) => {
//      array[index] = number + 1;
//    });

//    return (
//      <div>
//        <h2> Using map to create a duplicate array with modifications</h2>
//        <p>Original Array: {originalArray.join(', ')}</p>
//        <p>Modified Array: {modifiedArray1.join(', ')}</p>

//        <h2> Using map to modify the original array in place</h2>
//        <p>Modified Array (Original Array after map): {originalArray.join(' , ')}</p>
//      </div>
//    );
//  }

//  export default Mapfunct;
